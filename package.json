{
  "name": "angular-module-federation-test",
  "version": "0.0.1",
  "scripts": {
    "i": "yarn && cd platform && yarn",
    "start": "cd platform && yarn start"
  },
  "packageManager": "yarn@4.6.0",
  "private": true,
  "devDependencies": {
    "eslint": "^9.12.0",
    "prettier": "^3.3.3"
  }
}
